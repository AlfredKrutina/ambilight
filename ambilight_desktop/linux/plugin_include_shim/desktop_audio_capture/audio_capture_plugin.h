/* Shim for desktop_audio_capture 0.0.2: Flutter generates
 * #include <desktop_audio_capture/audio_capture_plugin.h>
 * but the plugin ships headers under include/audio_capture/. */
#ifndef AMBILIGHT_DESKTOP_AUDIO_CAPTURE_PATH_SHIM_H_
#define AMBILIGHT_DESKTOP_AUDIO_CAPTURE_PATH_SHIM_H_

#include <audio_capture/audio_capture_plugin.h>

#endif /* AMBILIGHT_DESKTOP_AUDIO_CAPTURE_PATH_SHIM_H_ */
